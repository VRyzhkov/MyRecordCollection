# MyRecordCollection

Нативное Android-приложение для просмотра плейлистов и любимых альбомов из Яндекс Музыки.

Для пользования приложением у вас должен быть доступ к яндекс музыке

Коллекция отображается в единой интерактивной карусели в следующем порядке:

1. **«Мне нравится»** — одна карточка для всех понравившихся треков, всегда первая.
2. **«Мои плейлисты»** — созданные пользователем, затем сохранённые плейлисты в порядке ответа Яндекс Музыки, без повторов.
3. **«Любимые альбомы»** — альбомы, отмеченные как любимые, сгруппированные по исполнителям; исполнители и названия альбомов отсортированы по алфавиту.

Центральная обложка увеличивается, а сверху показываются название раздела или исполнитель и название выбранной карточки. Нажатие открывает соответствующий плейлист или альбом в официальном приложении Яндекс Музыки или в браузере. Треки из «Мне нравится» не превращаются в отдельные карточки альбомов. После успешного обновления порядок и обложки сохраняются локально; аудиозаписи приложение не скачивает.

> Интеграция использует неофициальный API Яндекс Музыки. Его формат и доступность могут измениться без предупреждения.

## Возможности

- вход через сайт Яндекса по OAuth Device Flow без ручного копирования токена;
- безопасное хранение access- и refresh-токенов с помощью Android Keystore;
- единая карусель всех альбомов с поддержкой жестов и инерции;
- загрузка обложек альбомов;
- локальное хранение коллекции в Room для работы без сети;
- обновление коллекции жестом вниз;
- автоматическое удаление локальных данных и обложек исключённых альбомов;
- системная, светлая и тёмная темы;
- переход к альбому в Яндекс Музыке;
- автоматическая сборка подписанного APK при публикации Git-тега.
- проверка последнего GitHub Release при запуске, загрузка APK с отображением состояния и запуск системного установщика.
- обучение кнопок автомобильного руля для навигации по альбомам и исполнителям.

## Технологии

- Kotlin;
- Jetpack Compose и Material 3;
- ViewModel, Coroutines и StateFlow;
- Room;
- Coil;
- Android Keystore;
- GitHub Actions.

Минимальная версия Android — 8.0 (API 26).

## Запуск проекта

1. Установите Android Studio и Android SDK.
2. Откройте корень репозитория в Android Studio.
3. Дождитесь завершения Gradle Sync.
4. Создайте или выберите Android-эмулятор либо подключите телефон с включённой отладкой по USB.
5. Запустите конфигурацию `app`.

Сборка из командной строки на Windows:

```powershell
.\gradlew.bat assembleDebug
```

Готовый debug APK находится в каталоге:

```text
app/build/outputs/apk/debug/app-debug.apk
```

## Тестирование

Unit-тесты:

```powershell
.\gradlew.bat testDebugUnitTest
```

Инструментальные тесты на подключённом устройстве или запущенном эмуляторе:

```powershell
.\gradlew.bat connectedDebugAndroidTest
```

## Обновление приложения

При обнаружении новой версии нажмите «Скачать». APK загружается системным менеджером Android; приложение показывает ход загрузки, ожидание сети или ошибку с возможностью повторить попытку. Загрузку можно отменить.

После завершения вернитесь в MyRecordCollection и нажмите «Установить». При первом обновлении Android может попросить разрешить установку из этого источника. Разрешите её для MyRecordCollection и вернитесь в приложение: откроется системный установщик, в котором нужно подтвердить обновление. Если установка отменена, её можно запустить повторно без скачивания файла. Состояние загрузки сохраняется при перезапуске приложения.

Обновление поверх установленной версии должно быть подписано тем же ключом. Для обновления релизной версии используйте APK из GitHub Release, подписанный постоянным ключом проекта.

## Публикация APK

Workflow `.github/workflows/release-apk.yml` запускается при отправке тега формата `v0.0.1`. Он выполняет unit-тесты, собирает подписанный release APK и создаёт GitHub Release, из которого APK можно скачать на телефон.

Для подписи необходимо один раз создать постоянный файл `release.jks` и добавить в `Settings → Secrets and variables → Actions` четыре repository secret:

- `RELEASE_KEYSTORE_BASE64` — файл `release.jks`, преобразованный в Base64;
- `RELEASE_KEYSTORE_PASSWORD` — пароль хранилища;
- `RELEASE_KEY_ALIAS` — alias ключа, например `myrecordcollection`;
- `RELEASE_KEY_PASSWORD` — пароль закрытого ключа.

Пример создания ключа:

```powershell
& "C:\Program Files\Android\Android Studio\jbr\bin\keytool.exe" `
  -genkeypair `
  -v `
  -keystore .\release.jks `
  -alias myrecordcollection `
  -keyalg RSA `
  -keysize 2048 `
  -validity 10000
```

Преобразование ключа в Base64 и копирование результата в буфер обмена:

```powershell
[Convert]::ToBase64String(
    [IO.File]::ReadAllBytes((Resolve-Path .\release.jks))
) | Set-Clipboard
```

После настройки секретов создайте и отправьте тег:

```powershell
git tag v0.0.1
git push origin v0.0.1
```

Файл `release.jks` и пароли нельзя добавлять в Git. Необходимо хранить резервную копию ключа: без него Android не позволит установить будущую версию поверх ранее установленного приложения.

## Дополнительная документация

Текущая архитектура, принятые решения и план дальнейшей разработки описаны в [`PROJECT_NOTES.md`](PROJECT_NOTES.md).

## Участие в разработке

Сообщения об ошибках и предложения улучшений можно отправлять через [Issues](https://github.com/VRyzhkov/MyRecordCollection/issues/new/choose), выбрав подходящий шаблон. Правила подготовки изменений и отправки pull request описаны в [CONTRIBUTING.md](CONTRIBUTING.md).

## Поддержать проект

Если приложение вам полезно, вы можете добровольно поддержать его развитие:

- [Поддержать через CloudTips](https://pay.cloudtips.ru/p/c7151242).
- [Посмотреть адрес для поддержки в BscScan](https://bscscan.io/address/0x90E87117C67344a7daeD7cb8d1d03267de03FF08).

## Лицензия

Проект распространяется под GNU GPL версии 3.0. Полный текст лицензии — в файле [LICENSE](LICENSE).
